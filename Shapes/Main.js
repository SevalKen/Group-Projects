

function pallet(displacement){
    
    function drawShape(choice){
        switch(choice){
            case 1: //rectangle
                drawRectangle();
            case 2: //Triangle
                drawTriangle();
        }
    }
}
let offset = 2;
pallet(offset);
